# 核心依赖
pywebview>=4.0
opencv-python>=4.8.0
pillow>=10.0.0
pyautogui>=0.9.54
mss>=9.0.1
numpy>=1.24.0
pynput>=1.7.6
python-dotenv>=1.0.0
psutil>=5.9.0
pywin32>=306

# OCR 引擎（按需选择其一）
# PaddleOCR（推荐，中文识别好）
paddleocr>=2.7.0
# EasyOCR（更轻量，注释掉上面安装这个）
# easyocr>=1.7.0

# 游戏截图（可选但推荐）
# dxcam  # 需要编译，Windows 下可能有问题

# 打包
pyinstaller>=6.0.0
